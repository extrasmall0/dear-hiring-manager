# dear-hiring-manager — secrets. GITIGNORED, never committed.
#
# Optional: only needed for /dear-hiring-manager:apify-collect (Apify job discovery).
# Fill these IN A TEXT EDITOR — never paste your token into the chat.
#
#   open ~/.dear-hiring-manager/.env
#
# Get a token at https://console.apify.com/account/integrations, and pick a job-scraper actor.
APIFY_TOKEN=
APIFY_ACTOR=
