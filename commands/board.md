---
description: Show your job-application tracker board (grouped by status), or update an application's status.
argument-hint: [optional: "<company> -> <status>" to update, e.g. "Transcend -> interview"]
---

Use the **board** skill to render the application tracker from `~/.dear-hiring-manager/applications.md`,
and apply this status update if one is given:

$ARGUMENTS
