---
description: Fill multiple job applications from a list of URLs — each in its own browser tab, all stopped before Submit for one combined review.
argument-hint: <url1> <url2> ...  (or leave empty to read ~/.dear-hiring-manager/urls.txt)
---

Use the **batch** skill to fill each of these job URLs in its own tab, stopping before Submit on each:

$ARGUMENTS
