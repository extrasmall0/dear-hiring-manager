---
description: Collect job-posting URLs into ~/.dear-hiring-manager/urls.txt via Apify (run a scraper actor, or ingest an Apify CSV/JSON export), for /batch.
argument-hint: [role/location override]  or  [path to an Apify export]  (blank = use your profile targets)
---

Use the **apify-collect** skill to gather job-posting URLs into `urls.txt` via Apify:

$ARGUMENTS
