---
description: Collect job-posting URLs into ~/.dear-hiring-manager/urls.txt by running your Apify job scraper, for /batch.
argument-hint: [role/location override]  (blank = use your profile targets; needs APIFY_TOKEN in ~/.dear-hiring-manager/.env)
---

Use the **apify-collect** skill to gather job-posting URLs into `urls.txt` via Apify:

$ARGUMENTS
